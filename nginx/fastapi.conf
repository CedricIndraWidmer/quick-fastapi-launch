server {
    listen 80;
    server_name _; # Matches any request to your Public IP

    # Route to Staging (PM2 port 8000)
    location /staging/ {
        proxy_pass http://127.0.0.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Route to Production (PM2 port 8001)
    location /prod/ {
        proxy_pass http://127.0.0.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
